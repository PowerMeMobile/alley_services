language: erlang
script: "make test"
otp_release:
  - R16B03-1
branches:
  only:
    - master
notifications:
  email:
    - d.klionsky@dev.powermemobile.com
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq uuid-dev
